# fledge.toml — project task definitions
[tasks]
typecheck = "bun x tsc --noEmit -p tsconfig.json"
validation = "bun test tests/exercises/validation.test.ts"
javascript = "bun test tests/exercises/syntax/javascript.test.ts"
python = "bun test tests/exercises/syntax/python.test.ts"
rust = "bun test tests/exercises/syntax/rust.test.ts"
swift = "bun test tests/exercises/syntax/swift.test.ts --timeout 300000"
kotlin = "bun test tests/exercises/syntax/kotlin.test.ts --timeout 300000"
web = "bun test tests/exercises/syntax/html-css.test.ts"
build = "bun run build:prod"
governance = "bun test tests/governance/trust-rollout.test.ts"
toolchains = "command -v bun && command -v python3 && command -v rustc && command -v java && command -v kotlinc"

[lanes.verify]
description = "Validate governance, Linux language checks, TypeScript, and the production application build"
steps = ["governance", "toolchains", "validation", "javascript", "python", "rust", "kotlin", "web", "typecheck", "build"]
